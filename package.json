{
  "name": "@kikiutils/types",
  "version": "3.2.2",
  "description": "A versatile library of utility types for TypeScript.",
  "author": "kiki-kanri",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kiki-kanri/kikiutils-node-types.git"
  },
  "keywords": [
    "types"
  ],
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "release": "pnpm run lint && pnpm run build && changelogen --no-output --push --release && sh ./build-and-publish.sh"
  },
  "dependencies": {
    "bson": "^6.10.1",
    "type-fest": "^4.30.1"
  },
  "devDependencies": {
    "@kikiutils/changelogen": "^0.7.0",
    "@kikiutils/eslint-config": "^0.7.1",
    "@kikiutils/tsconfigs": "^3.0.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2",
    "vue": "^3.5.13"
  }
}
